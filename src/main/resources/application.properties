# Generated by GitHub Copilot
spring.application.name=shelter-testing

# H2 in-memory database
spring.datasource.url=jdbc:h2:mem:shelterdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# H2 Console (for local development only)
spring.h2.console.enabled=true

# Partner registry client (EXT-1) — RestClient connects lazily, so no live
# server is required for the app/context to start.
shelter.partner-registry.base-url=http://localhost:8089
